// $Id$
// vim:ft=javascript
// config.w32 for extension nlpir

// If your extension references something external, use ARG_WITH
ARG_WITH("nlpir", "for nlpir support", "no");

// Otherwise, use ARG_ENABLE
ARG_ENABLE("nlpir", "enable nlpir support", "no");

if (PHP_NLPIR != "no") {
    // TODO(wangyi): Add some test code to ensure libnlpir is exist.
	EXTENSION("nlpir", "nlpir.c");
}